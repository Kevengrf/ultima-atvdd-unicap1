# IMPORTANTE: Substitua a URL abaixo pela sua URL de conexão real do Supabase.
# A URL atual é um valor fictício apenas para permitir que o servidor inicie.
DATABASE_URL="postgresql://postgres:postgres@db.example.com:5432/postgres"

# Segredo para a geração de JSON Web Tokens (JWT)
# Em um ambiente de produção, use um valor longo e aleatório
JWT_SECRET="seu-segredo-super-secreto-aqui"
